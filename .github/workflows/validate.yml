name: Validate

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v3"
      - name: HACS Validation
        uses: "hacs/action@main"
        with:
          category: "theme"
      - name: Build
        run: |
          yarn install
          yarn build
